const sans = document.getElementById('newSans')
sans.addEventListener

function changePictureInitially() {
  danceCount = danceCount + 1
  document.getElementById().src =  `dancingSans${danceCount}`
  setTimeout(() => { 
    document.getElementById('dancing').id = 'newSans'}, 500 );
  
  
}
